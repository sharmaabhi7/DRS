<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/user.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <h1 class="text-xl font-bold bg-zinc-500 flex">Welcome, <%= user.name %>
    <div>
        <a href="/login"><button class="logout">Logout</button></a>
    </div>
    </h1>
    <div class="text-3xl text-center">
        YOUR PERSONAL DETAILS
    </div>
    <div class="details">
        Full Name: <%= user.name %>
        Age: <%= user.number %>
    </div>
    <% if (user.number < 18) { %>
        <div class="font-bold text-3xl">You Have to chose  Premimum your preiemum diet plan</div>
      <% } else { %>
        <div class="font-bold text-3xl">You Have to chose Your star diet plan</div>
      <% } %>
  
</body>
</html>